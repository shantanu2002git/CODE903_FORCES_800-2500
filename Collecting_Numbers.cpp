#include <bits/stdc++.h>
using namespace std;
int main()
{
 long long int n, mx = INT_MIN;
  cin >> n;
 vector<int>arr(n),bc;
  for(int i = 0; i < n; i++){
    cin >> arr[i];
  }
  int mi=*min_element(arr.begin(),arr.end());
  int c=0;
  bc.push_back(mi);
  while(true){
while(i<n){
  if(bc.back())
}
  }
  return 0;
}
